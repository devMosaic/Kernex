services:
  - type: web
    name: kernex
    runtime: docker
    plan: free
    region: oregon
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: kernex-data
      mountPath: /app/data
      sizeGB: 1
